line-length = 100

[lint]
# defaults (E4/E7/E9/F) plus nothing exotic - the codebase is plain Python
[lint.per-file-ignores]
# the tests must extend sys.path before importing the project modules
"tests/*" = ["E402"]
